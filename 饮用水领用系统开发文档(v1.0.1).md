# 饮用水领用系统开发文档 v1.0.1

## 1. 项目概述

饮用水领用系统是一个集送水管理、库存管理、领用管理、账单管理和统计分析于一体的综合性系统，旨在提高企业饮用水管理的效率和透明度。系统分为三个主要部分：后端API服务、前端管理界面和微信小程序。

### 1.1 系统功能

- **用户管理**：用户注册、登录、权限管理
- **送水管理**：送水记录的创建、审核、查询
- **库存管理**：库存查询、库存预警、库存调整
- **领用管理**：领用申请、审批、记录查询
- **账单管理**：账单生成、支付状态管理、账单查询
- **统计分析**：用水量统计、费用统计、趋势分析
- **系统管理**：部门管理、仓库管理、饮用水品类管理
- **小程序端**：送水员和领用人的移动操作界面

### 1.2 系统架构

系统采用前后端分离架构，具体如下：

- **后端**：Node.js + Express + Sequelize + MySQL
- **前端**：Vue 3 + Vite + Vue Router + Axios
- **小程序**：微信小程序原生开发
- **数据库**：MySQL

## 2. 技术栈

### 2.1 后端技术

| 技术 | 版本 | 用途 |
|------|------|------|
| Node.js | 14+ | 运行环境 |
| Express | 4.18.2 | Web框架 |
| Sequelize | 6.35.2 | ORM框架 |
| MySQL | 8.0+ | 数据库 |
| bcrypt | 5.1.1 | 密码加密 |
| jsonwebtoken | 9.0.2 | JWT认证 |
| cors | 2.8.5 | 跨域处理 |
| dotenv | 16.3.1 | 环境变量管理 |

### 2.2 前端技术

| 技术 | 版本 | 用途 |
|------|------|------|
| Vue | 3.3.8 | 前端框架 |
| Vite | 4.5.0 | 构建工具 |
| Vue Router | 4.2.5 | 路由管理 |
| Axios | 1.6.0 | HTTP请求 |
| Element Plus | 2.4.4 | UI组件库 |

### 2.3 小程序技术

| 技术 | 版本 | 用途 |
|------|------|------|
| 微信小程序 | 基础库 2.29.4 | 运行环境 |
| wx.request | - | 网络请求 |
| wx.login | - | 登录认证 |

## 3. 系统架构

### 3.1 后端架构

后端采用分层架构设计，具体如下：

1. **路由层**：处理HTTP请求，路由分发
2. **服务层**：业务逻辑处理
3. **模型层**：数据模型定义和数据库操作
4. **中间件**：认证、错误处理等
5. **配置层**：数据库配置、环境变量等

### 3.2 前端架构

前端采用Vue 3的组合式API架构，具体如下：

1. **视图层**：Vue组件和页面
2. **路由层**：Vue Router管理路由
3. **工具层**：API调用、工具函数等
4. **状态管理**：Vue 3的响应式API

### 3.3 小程序架构

小程序采用微信小程序原生架构，具体如下：

1. **页面层**：小程序页面
2. **工具层**：API调用、工具函数等
3. **配置层**：小程序配置

## 4. 数据库设计

### 4.1 数据库表结构

#### 4.1.1 用户表 (users)

| 字段名 | 数据类型 | 约束 | 描述 |
|-------|---------|------|------|
| id | INTEGER | PRIMARY KEY, AUTO_INCREMENT | 用户ID |
| username | VARCHAR(50) | UNIQUE, NOT NULL | 用户名 |
| password | VARCHAR(255) | NOT NULL | 密码哈希 |
| name | VARCHAR(50) | NOT NULL | 姓名 |
| email | VARCHAR(100) | UNIQUE | 邮箱 |
| phone | VARCHAR(20) | UNIQUE | 手机号 |
| departmentId | INTEGER | FOREIGN KEY | 部门ID |
| role | ENUM('admin', 'user', 'deliveryman') | NOT NULL | 角色 |
| status | TINYINT | DEFAULT 1 | 状态(1:启用, 0:禁用) |
| createdAt | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updatedAt | DATETIME | DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

#### 4.1.2 部门表 (departments)

| 字段名 | 数据类型 | 约束 | 描述 |
|-------|---------|------|------|
| id | INTEGER | PRIMARY KEY, AUTO_INCREMENT | 部门ID |
| name | VARCHAR(50) | UNIQUE, NOT NULL | 部门名称 |
| createdAt | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updatedAt | DATETIME | DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

#### 4.1.3 饮用水品类表 (water_categories)

| 字段名 | 数据类型 | 约束 | 描述 |
|-------|---------|------|------|
| id | INTEGER | PRIMARY KEY, AUTO_INCREMENT | 品类ID |
| name | VARCHAR(50) | UNIQUE, NOT NULL | 品类名称 |
| unit | VARCHAR(20) | NOT NULL | 单位 |
| price | DECIMAL(10,2) | NOT NULL | 单价 |
| capacity | DECIMAL(10,2) | NOT NULL | 容量 |
| createdAt | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updatedAt | DATETIME | DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

#### 4.1.4 仓库表 (warehouses)

| 字段名 | 数据类型 | 约束 | 描述 |
|-------|---------|------|------|
| id | INTEGER | PRIMARY KEY, AUTO_INCREMENT | 仓库ID |
| name | VARCHAR(50) | UNIQUE, NOT NULL | 仓库名称 |
| location | VARCHAR(255) | NOT NULL | 仓库位置 |
| contactPerson | VARCHAR(50) | NOT NULL | 联系人 |
| contactPhone | VARCHAR(20) | NOT NULL | 联系电话 |
| createdAt | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updatedAt | DATETIME | DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

#### 4.1.5 库存表 (inventories)

| 字段名 | 数据类型 | 约束 | 描述 |
|-------|---------|------|------|
| id | INTEGER | PRIMARY KEY, AUTO_INCREMENT | 库存ID |
| waterCategoryId | INTEGER | FOREIGN KEY | 饮用水品类ID |
| warehouseId | INTEGER | FOREIGN KEY | 仓库ID |
| quantity | INTEGER | NOT NULL | 库存量 |
| emptyBucketQuantity | INTEGER | DEFAULT 0 | 空桶数量 |
| location | VARCHAR(255) | NOT NULL | 存储位置 |
| alertThreshold | INTEGER | NOT NULL | 预警阈值 |
| createdAt | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updatedAt | DATETIME | DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

#### 4.1.6 送水记录表 (delivery_records)

| 字段名 | 数据类型 | 约束 | 描述 |
|-------|---------|------|------|
| id | INTEGER | PRIMARY KEY, AUTO_INCREMENT | 记录ID |
| waterCategoryId | INTEGER | FOREIGN KEY | 饮用水品类ID |
| warehouseId | INTEGER | FOREIGN KEY | 仓库ID |
| quantity | INTEGER | NOT NULL | 送水数量 |
| emptyBucketQuantity | INTEGER | DEFAULT 0 | 空桶数量 |
| date | DATE | NOT NULL | 送水日期 |
| remark | VARCHAR(255) | | 备注 |
| status | ENUM('PENDING', 'APPROVED', 'REJECTED') | DEFAULT 'PENDING' | 状态 |
| createdAt | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updatedAt | DATETIME | DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

#### 4.1.7 领用记录表 (consumptions)

| 字段名 | 数据类型 | 约束 | 描述 |
|-------|---------|------|------|
| id | INTEGER | PRIMARY KEY, AUTO_INCREMENT | 记录ID |
| waterCategoryId | INTEGER | FOREIGN KEY | 饮用水品类ID |
| warehouseId | INTEGER | FOREIGN KEY | 仓库ID |
| departmentId | INTEGER | FOREIGN KEY | 部门ID |
| receiver | VARCHAR(50) | NOT NULL | 领用人 |
| quantity | INTEGER | NOT NULL | 领用数量 |
| returnEmptyBottles | INTEGER | DEFAULT 0 | 归还空瓶数量 |
| consumptionDate | DATE | NOT NULL | 领用日期 |
| notes | VARCHAR(255) | | 备注 |
| status | ENUM('PENDING', 'APPROVED', 'REJECTED') | DEFAULT 'PENDING' | 状态 |
| createdAt | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updatedAt | DATETIME | DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

#### 4.1.8 账单表 (bills)

| 字段名 | 数据类型 | 约束 | 描述 |
|-------|---------|------|------|
| id | INTEGER | PRIMARY KEY, AUTO_INCREMENT | 账单ID |
| billNo | VARCHAR(50) | UNIQUE, NOT NULL | 账单编号 |
| startDate | DATE | NOT NULL | 开始日期 |
| endDate | DATE | NOT NULL | 结束日期 |
| totalAmount | DECIMAL(10,2) | NOT NULL | 总金额 |
| status | ENUM('UNPAID', 'PAID', 'CANCELLED') | DEFAULT 'UNPAID' | 状态 |
| paymentMethod | VARCHAR(50) | | 支付方式 |
| paidAt | DATETIME | | 支付时间 |
| createdAt | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updatedAt | DATETIME | DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

#### 4.1.9 支付记录表 (payments)

| 字段名 | 数据类型 | 约束 | 描述 |
|-------|---------|------|------|
| id | INTEGER | PRIMARY KEY, AUTO_INCREMENT | 支付ID |
| billId | INTEGER | FOREIGN KEY | 账单ID |
| amount | DECIMAL(10,2) | NOT NULL | 支付金额 |
| paymentMethod | VARCHAR(50) | NOT NULL | 支付方式 |
| transactionId | VARCHAR(100) | UNIQUE | 交易ID |
| paymentDate | DATETIME | NOT NULL | 支付日期 |
| createdAt | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updatedAt | DATETIME | DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

#### 4.1.10 系统日志表 (system_logs)

| 字段名 | 数据类型 | 约束 | 描述 |
|-------|---------|------|------|
| id | INTEGER | PRIMARY KEY, AUTO_INCREMENT | 日志ID |
| userId | INTEGER | FOREIGN KEY | 用户ID |
| action | VARCHAR(100) | NOT NULL | 操作类型 |
| module | VARCHAR(50) | NOT NULL | 操作模块 |
| description | VARCHAR(255) | NOT NULL | 操作描述 |
| ipAddress | VARCHAR(50) | | IP地址 |
| createdAt | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

### 4.2 数据库初始化

系统启动时会自动执行数据库初始化脚本，创建表结构并初始化基础数据：

- **部门数据**：行政部、技术部、市场部、财务部
- **用户数据**：管理员(admin/123456)、普通用户(user/123456)、送水员(deliveryman/123456)
- **饮用水品类数据**：桶装水、瓶装水、矿泉水
- **仓库数据**：仓库A、仓库B、仓库C
- **库存数据**：各仓库初始库存
- **送水记录数据**：模拟送水记录
- **领用记录数据**：模拟领用记录
- **账单数据**：模拟账单数据

## 5. 后端实现

### 5.1 目录结构

```
backend/
├── config/              # 配置文件
│   ├── database.js      # 数据库配置
│   └── init-db.js       # 数据库初始化
├── middleware/          # 中间件
│   └── auth.js          # 认证中间件
├── models/              # 数据模型
│   ├── Bill.js          # 账单模型
│   ├── Consumption.js   # 领用记录模型
│   ├── DeliveryRecord.js # 送水记录模型
│   ├── Department.js    # 部门模型
│   ├── Inventory.js     # 库存模型
│   ├── Payment.js       # 支付记录模型
│   ├── SystemLog.js     # 系统日志模型
│   ├── User.js          # 用户模型
│   ├── Warehouse.js     # 仓库模型
│   └── WaterCategory.js # 饮用水品类模型
├── routes/              # 路由
│   ├── auth.js          # 认证路由
│   ├── billing.js       # 账单路由
│   ├── consumption.js   # 领用路由
│   ├── delivery.js      # 送水路由
│   ├── inventory.js     # 库存路由
│   ├── mini-app.js      # 小程序路由
│   ├── statistics.js    # 统计路由
│   └── system.js        # 系统路由
├── services/            # 服务层
│   ├── BillingService.js      # 账单服务
│   ├── ConsumptionService.js  # 领用服务
│   ├── DeliveryService.js     # 送水服务
│   ├── DepartmentService.js   # 部门服务
│   ├── InventoryService.js    # 库存服务
│   ├── StatisticsService.js   # 统计服务
│   ├── SystemService.js       # 系统服务
│   ├── UserService.js         # 用户服务
│   └── WarehouseService.js    # 仓库服务
├── app.js               # 应用入口
├── package-lock.json    # 依赖锁文件
└── package.json         # 依赖配置
```

### 5.2 API 接口

#### 5.2.1 认证接口

| API路径 | 方法 | 功能 | 请求体 | 响应 |
|---------|------|------|--------|------|
| `/api/auth/login` | POST | 用户登录 | `{username, password}` | `{code, message, data: {token, user}}` |
| `/api/auth/register` | POST | 用户注册 | `{username, password, name, email, phone, departmentId, role}` | `{code, message, data: user}` |
| `/api/auth/refresh` | POST | 刷新token | `{token}` | `{code, message, data: {token}}` |

#### 5.2.2 送水接口

| API路径 | 方法 | 功能 | 请求体 | 响应 |
|---------|------|------|--------|------|
| `/api/delivery` | GET | 获取送水记录列表 | N/A | `{code, message, data: [deliveryRecords]}` |
| `/api/delivery` | POST | 创建送水记录 | `{waterCategoryId, warehouseId, quantity, emptyBucketQuantity, date, remark}` | `{code, message, data: deliveryRecord}` |
| `/api/delivery/:id` | GET | 获取送水记录详情 | N/A | `{code, message, data: deliveryRecord}` |
| `/api/delivery/:id` | PUT | 更新送水记录 | `{waterCategoryId, warehouseId, quantity, emptyBucketQuantity, date, remark, status}` | `{code, message, data: deliveryRecord}` |
| `/api/delivery/:id` | DELETE | 删除送水记录 | N/A | `{code, message}` |
| `/api/delivery/:id/approve` | POST | 审批送水记录 | N/A | `{code, message, data: deliveryRecord}` |
| `/api/delivery/:id/reject` | POST | 拒绝送水记录 | N/A | `{code, message, data: deliveryRecord}` |

#### 5.2.3 库存接口

| API路径 | 方法 | 功能 | 请求体 | 响应 |
|---------|------|------|--------|------|
| `/api/inventory` | GET | 获取库存列表 | N/A | `{code, message, data: [inventories]}` |
| `/api/inventory/:id` | GET | 获取库存详情 | N/A | `{code, message, data: inventory}` |
| `/api/inventory/:id` | PUT | 更新库存 | `{quantity, emptyBucketQuantity, alertThreshold}` | `{code, message, data: inventory}` |
| `/api/inventory/alert` | GET | 获取库存预警 | N/A | `{code, message, data: [inventories]}` |

#### 5.2.4 领用接口

| API路径 | 方法 | 功能 | 请求体 | 响应 |
|---------|------|------|--------|------|
| `/api/consumption` | GET | 获取领用记录列表 | N/A | `{code, message, data: [consumptions]}` |
| `/api/consumption` | POST | 创建领用记录 | `{waterCategoryId, warehouseId, departmentId, receiver, quantity, returnEmptyBottles, consumptionDate, notes}` | `{code, message, data: consumption}` |
| `/api/consumption/:id` | GET | 获取领用记录详情 | N/A | `{code, message, data: consumption}` |
| `/api/consumption/:id` | PUT | 更新领用记录 | `{waterCategoryId, warehouseId, departmentId, receiver, quantity, returnEmptyBottles, consumptionDate, notes, status}` | `{code, message, data: consumption}` |
| `/api/consumption/:id` | DELETE | 删除领用记录 | N/A | `{code, message}` |
| `/api/consumption/:id/approve` | POST | 审批领用记录 | N/A | `{code, message, data: consumption}` |
| `/api/consumption/:id/reject` | POST | 拒绝领用记录 | N/A | `{code, message, data: consumption}` |

#### 5.2.5 账单接口

| API路径 | 方法 | 功能 | 请求体 | 响应 |
|---------|------|------|--------|------|
| `/api/billing` | GET | 获取账单列表 | N/A | `{code, message, data: [bills]}` |
| `/api/billing` | POST | 创建账单 | `{billNo, startDate, endDate, totalAmount, status}` | `{code, message, data: bill}` |
| `/api/billing/:id` | GET | 获取账单详情 | N/A | `{code, message, data: bill}` |
| `/api/billing/:id` | PUT | 更新账单 | `{billNo, startDate, endDate, totalAmount, status, paymentMethod, paidAt}` | `{code, message, data: bill}` |
| `/api/billing/:id` | DELETE | 删除账单 | N/A | `{code, message}` |
| `/api/billing/:id/pay` | POST | 支付账单 | `{paymentMethod, transactionId}` | `{code, message, data: bill}` |

#### 5.2.6 统计接口

| API路径 | 方法 | 功能 | 请求体 | 响应 |
|---------|------|------|--------|------|
| `/api/statistics/consumption` | GET | 获取用水量统计 | N/A | `{code, message, data: statistics}` |
| `/api/statistics/cost` | GET | 获取费用统计 | N/A | `{code, message, data: statistics}` |
| `/api/statistics/trend` | GET | 获取趋势分析 | N/A | `{code, message, data: statistics}` |
| `/api/statistics/department` | GET | 获取部门统计 | N/A | `{code, message, data: statistics}` |
| `/api/statistics/inventory` | GET | 获取库存统计 | N/A | `{code, message, data: statistics}` |

#### 5.2.7 系统接口

| API路径 | 方法 | 功能 | 请求体 | 响应 |
|---------|------|------|--------|------|
| `/api/system/users` | GET | 获取用户列表 | N/A | `{code, message, data: [users]}` |
| `/api/system/users` | POST | 创建用户 | `{username, password, name, email, phone, departmentId, role, status}` | `{code, message, data: user}` |
| `/api/system/users/:id` | GET | 获取用户详情 | N/A | `{code, message, data: user}` |
| `/api/system/users/:id` | PUT | 更新用户 | `{username, password, name, email, phone, departmentId, role, status}` | `{code, message, data: user}` |
| `/api/system/users/:id` | DELETE | 删除用户 | N/A | `{code, message}` |
| `/api/system/departments` | GET | 获取部门列表 | N/A | `{code, message, data: [departments]}` |
| `/api/system/departments` | POST | 创建部门 | `{name}` | `{code, message, data: department}` |
| `/api/system/departments/:id` | PUT | 更新部门 | `{name}` | `{code, message, data: department}` |
| `/api/system/departments/:id` | DELETE | 删除部门 | N/A | `{code, message}` |
| `/api/system/warehouses` | GET | 获取仓库列表 | N/A | `{code, message, data: [warehouses]}` |
| `/api/system/warehouses` | POST | 创建仓库 | `{name, location, contactPerson, contactPhone}` | `{code, message, data: warehouse}` |
| `/api/system/warehouses/:id` | PUT | 更新仓库 | `{name, location, contactPerson, contactPhone}` | `{code, message, data: warehouse}` |
| `/api/system/warehouses/:id` | DELETE | 删除仓库 | N/A | `{code, message}` |
| `/api/system/water-categories` | GET | 获取饮用水品类列表 | N/A | `{code, message, data: [waterCategories]}` |
| `/api/system/water-categories` | POST | 创建饮用水品类 | `{name, unit, price, capacity}` | `{code, message, data: waterCategory}` |
| `/api/system/water-categories/:id` | PUT | 更新饮用水品类 | `{name, unit, price, capacity}` | `{code, message, data: waterCategory}` |
| `/api/system/water-categories/:id` | DELETE | 删除饮用水品类 | N/A | `{code, message}` |
| `/api/system/logs` | GET | 获取系统日志 | N/A | `{code, message, data: [logs]}` |

#### 5.2.8 小程序接口

| API路径 | 方法 | 功能 | 请求体 | 响应 |
|---------|------|------|--------|------|
| `/api/mini-app/login` | POST | 小程序登录 | `{code}` | `{code, message, data: {token, user}}` |
| `/api/mini-app/delivery` | GET | 获取送水记录列表 | N/A | `{code, message, data: [deliveryRecords]}` |
| `/api/mini-app/delivery` | POST | 创建送水记录 | `{waterCategoryId, warehouseId, quantity, emptyBucketQuantity, date, remark}` | `{code, message, data: deliveryRecord}` |
| `/api/mini-app/consumption` | GET | 获取领用记录列表 | N/A | `{code, message, data: [consumptions]}` |
| `/api/mini-app/consumption` | POST | 创建领用记录 | `{waterCategoryId, warehouseId, departmentId, receiver, quantity, returnEmptyBottles, consumptionDate, notes}` | `{code, message, data: consumption}` |

### 5.3 服务层实现

服务层负责处理业务逻辑，以下是主要服务的实现：

#### 5.3.1 送水服务 (DeliveryService.js)

- **createDeliveryRecord**：创建送水记录，同时更新库存
- **updateDeliveryRecord**：更新送水记录，同时更新库存
- **approveDeliveryRecord**：审批送水记录，更新状态和库存
- **rejectDeliveryRecord**：拒绝送水记录，更新状态
- **getDeliveryRecords**：获取送水记录列表
- **getDeliveryRecordById**：根据ID获取送水记录详情
- **deleteDeliveryRecord**：删除送水记录

#### 5.3.2 库存服务 (InventoryService.js)

- **getInventories**：获取库存列表
- **getInventoryById**：根据ID获取库存详情
- **updateInventory**：更新库存
- **getLowStockInventories**：获取低库存预警
- **adjustInventory**：调整库存

#### 5.3.3 领用服务 (ConsumptionService.js)

- **createConsumption**：创建领用记录，同时更新库存
- **updateConsumption**：更新领用记录，同时更新库存
- **approveConsumption**：审批领用记录，更新状态和库存
- **rejectConsumption**：拒绝领用记录，更新状态
- **getConsumptions**：获取领用记录列表
- **getConsumptionById**：根据ID获取领用记录详情
- **deleteConsumption**：删除领用记录

#### 5.3.4 账单服务 (BillingService.js)

- **createBill**：创建账单
- **updateBill**：更新账单
- **payBill**：支付账单，创建支付记录
- **getBills**：获取账单列表
- **getBillById**：根据ID获取账单详情
- **deleteBill**：删除账单

#### 5.3.5 统计服务 (StatisticsService.js)

- **getConsumptionStatistics**：获取用水量统计
- **getCostStatistics**：获取费用统计
- **getTrendAnalysis**：获取趋势分析
- **getDepartmentStatistics**：获取部门统计
- **getInventoryStatistics**：获取库存统计

## 6. 前端实现

### 6.1 目录结构

```
frontend/
├── public/              # 静态资源
├── src/                 # 源代码
│   ├── assets/          # 资源文件
│   ├── components/      # 组件
│   ├── router/          # 路由
│   │   └── index.js     # 路由配置
│   ├── utils/           # 工具函数
│   │   └── api.js       # API调用
│   ├── views/           # 页面视图
│   │   ├── HomeView.vue # 首页
│   │   ├── DeliveryView.vue # 送水管理
│   │   ├── InventoryView.vue # 库存管理
│   │   ├── ConsumptionView.vue # 领用管理
│   │   ├── BillingView.vue # 账单管理
│   │   ├── StatisticsView.vue # 统计分析
│   │   ├── SystemView.vue # 系统管理
│   │   ├── ProfileView.vue # 个人中心
│   │   └── LoginView.vue # 登录页面
│   ├── App.vue          # 应用入口组件
│   ├── main.js          # 应用入口
│   └── style.css        # 全局样式
├── .gitignore           # Git忽略文件
├── README.md            # 项目说明
├── index.html           # HTML模板
├── package-lock.json    # 依赖锁文件
├── package.json         # 依赖配置
└── vite.config.js       # Vite配置
```

### 6.2 路由配置

前端采用Vue Router进行路由管理，主要路由如下：

| 路径 | 组件 | 功能 | 权限要求 |
|------|------|------|----------|
| `/login` | LoginView.vue | 登录页面 | 无 |
| `/` | HomeView.vue | 首页 | 需要登录 |
| `/delivery` | DeliveryView.vue | 送水管理 | 需要登录 |
| `/inventory` | InventoryView.vue | 库存管理 | 需要登录 |
| `/consumption` | ConsumptionView.vue | 领用管理 | 需要登录 |
| `/billing` | BillingView.vue | 账单管理 | 需要登录 |
| `/statistics` | StatisticsView.vue | 统计分析 | 需要登录 |
| `/system` | SystemView.vue | 系统管理 | 需要登录 |
| `/profile` | ProfileView.vue | 个人中心 | 需要登录 |

### 6.3 主要页面实现

#### 6.3.1 登录页面 (LoginView.vue)

- **功能**：用户登录，表单验证，错误提示
- **实现**：使用Vue 3的组合式API，Axios调用登录接口

#### 6.3.2 送水管理页面 (DeliveryView.vue)

- **功能**：送水记录列表，创建送水记录，编辑送水记录，审批送水记录
- **实现**：使用Vue 3的组合式API，Axios调用送水接口，Element Plus组件库

#### 6.3.3 库存管理页面 (InventoryView.vue)

- **功能**：库存列表，库存详情，库存预警，库存调整
- **实现**：使用Vue 3的组合式API，Axios调用库存接口，Element Plus组件库

#### 6.3.4 领用管理页面 (ConsumptionView.vue)

- **功能**：领用记录列表，创建领用记录，编辑领用记录，审批领用记录
- **实现**：使用Vue 3的组合式API，Axios调用领用接口，Element Plus组件库

#### 6.3.5 账单管理页面 (BillingView.vue)

- **功能**：账单列表，创建账单，编辑账单，支付账单
- **实现**：使用Vue 3的组合式API，Axios调用账单接口，Element Plus组件库

#### 6.3.6 统计分析页面 (StatisticsView.vue)

- **功能**：用水量统计，费用统计，趋势分析，部门统计，库存统计
- **实现**：使用Vue 3的组合式API，Axios调用统计接口，ECharts图表库

#### 6.3.7 系统管理页面 (SystemView.vue)

- **功能**：用户管理，部门管理，仓库管理，饮用水品类管理，系统日志
- **实现**：使用Vue 3的组合式API，Axios调用系统接口，Element Plus组件库

### 6.4 API调用

前端使用Axios进行API调用，封装在api.js中，主要包括：

- **login**：登录
- **register**：注册
- **getDeliveryRecords**：获取送水记录
- **createDeliveryRecord**：创建送水记录
- **updateDeliveryRecord**：更新送水记录
- **deleteDeliveryRecord**：删除送水记录
- **approveDeliveryRecord**：审批送水记录
- **rejectDeliveryRecord**：拒绝送水记录
- **getInventories**：获取库存
- **updateInventory**：更新库存
- **getConsumptions**：获取领用记录
- **createConsumption**：创建领用记录
- **updateConsumption**：更新领用记录
- **deleteConsumption**：删除领用记录
- **approveConsumption**：审批领用记录
- **rejectConsumption**：拒绝领用记录
- **getBills**：获取账单
- **createBill**：创建账单
- **updateBill**：更新账单
- **deleteBill**：删除账单
- **payBill**：支付账单
- **getStatistics**：获取统计数据
- **getUsers**：获取用户
- **createUser**：创建用户
- **updateUser**：更新用户
- **deleteUser**：删除用户
- **getDepartments**：获取部门
- **createDepartment**：创建部门
- **updateDepartment**：更新部门
- **deleteDepartment**：删除部门
- **getWarehouses**：获取仓库
- **createWarehouse**：创建仓库
- **updateWarehouse**：更新仓库
- **deleteWarehouse**：删除仓库
- **getWaterCategories**：获取饮用水品类
- **createWaterCategory**：创建饮用水品类
- **updateWaterCategory**：更新饮用水品类
- **deleteWaterCategory**：删除饮用水品类
- **getSystemLogs**：获取系统日志

## 7. 小程序实现

### 7.1 目录结构

```
water-mini-app/
├── pages/               # 页面
│   ├── login/           # 登录页面
│   │   ├── index.js     # 页面逻辑
│   │   ├── index.json   # 页面配置
│   │   ├── index.wxml   # 页面结构
│   │   └── index.wxss   # 页面样式
│   ├── delivery/        # 送水页面
│   │   ├── index.js     # 页面逻辑
│   │   ├── index.json   # 页面配置
│   │   ├── index.wxml   # 页面结构
│   │   └── index.wxss   # 页面样式
│   ├── consumption/     # 领用页面
│   │   ├── index.js     # 页面逻辑
│   │   ├── index.json   # 页面配置
│   │   ├── index.wxml   # 页面结构
│   │   └── index.wxss   # 页面样式
│   └── success/         # 成功页面
│       ├── index.js     # 页面逻辑
│       ├── index.json   # 页面配置
│       ├── index.wxml   # 页面结构
│       └── index.wxss   # 页面样式
├── utils/               # 工具函数
│   ├── api.js           # API调用
│   ├── generate-b-warehouse-qr.js # 生成仓库二维码
│   └── qrcode-generator.js # 二维码生成器
├── app.js               # 应用入口
├── app.json             # 应用配置
├── app.wxss             # 全局样式
├── project.config.json  # 项目配置
├── project.private.config.json # 私有配置
├── qrcode-generator.html # 二维码生成页面
├── sitemap.json         # 站点地图
├── 送水员.png           # 送水员图标
└── 领用人.png           # 领用人图标
```

### 7.2 页面实现

#### 7.2.1 登录页面 (login/index.wxml)

- **功能**：小程序登录，扫码登录
- **实现**：使用微信登录API，调用后端登录接口

#### 7.2.2 送水页面 (delivery/index.wxml)

- **功能**：送水记录列表，创建送水记录，扫码识别
- **实现**：使用微信小程序原生组件，调用后端送水接口

#### 7.2.3 领用页面 (consumption/index.wxml)

- **功能**：领用记录列表，创建领用记录，扫码识别
- **实现**：使用微信小程序原生组件，调用后端领用接口

#### 7.2.4 成功页面 (success/index.wxml)

- **功能**：操作成功提示
- **实现**：使用微信小程序原生组件

### 7.3 API调用

小程序使用wx.request进行API调用，封装在api.js中，主要包括：

- **login**：登录
- **getDeliveryRecords**：获取送水记录
- **createDeliveryRecord**：创建送水记录
- **getConsumptions**：获取领用记录
- **createConsumption**：创建领用记录

## 8. 开发进度

### 8.1 已完成功能

#### 8.1.1 后端

- [x] 数据库设计和初始化
- [x] 用户认证和权限管理
- [x] 送水管理模块
- [x] 库存管理模块
- [x] 领用管理模块
- [x] 账单管理模块
- [x] 统计分析模块
- [x] 系统管理模块
- [x] 小程序接口

#### 8.1.2 前端

- [x] 登录页面
- [x] 首页
- [x] 送水管理页面
- [x] 库存管理页面
- [x] 领用管理页面
- [x] 账单管理页面
- [x] 统计分析页面
- [x] 系统管理页面
- [x] 个人中心页面
- [x] 路由配置和权限管理

#### 8.1.3 小程序

- [x] 登录页面
- [x] 送水页面
- [x] 领用页面
- [x] 成功页面
- [x] API调用封装

### 8.2 待完成功能

#### 8.2.1 后端

- [ ] 性能优化
- [ ] 安全性增强
- [ ] 日志系统完善
- [ ] 缓存机制

#### 8.2.2 前端

- [ ] 响应式设计优化
- [ ] 页面加载性能优化
- [ ] 交互体验改进
- [ ] 多语言支持

#### 8.2.3 小程序

- [ ] 扫码功能完善
- [ ] 离线操作支持
- [ ] 推送通知
- [ ] 用户体验优化

### 8.3 测试进度

- [x] 单元测试
- [x] 集成测试
- [ ] 性能测试
- [ ] 安全测试
- [ ] 用户验收测试

## 9. 部署方案

### 9.1 后端部署

1. **环境准备**：Node.js 14+, MySQL 8.0+
2. **数据库配置**：创建数据库，配置环境变量
3. **依赖安装**：`npm install`
4. **启动服务**：`npm start`
5. **PM2管理**：使用PM2进行进程管理

### 9.2 前端部署

1. **环境准备**：Node.js 14+
2. **依赖安装**：`npm install`
3. **构建项目**：`npm run build`
4. **部署静态资源**：将dist目录部署到Nginx或Apache

### 9.3 小程序部署

1. **代码提交**：将代码提交到微信小程序开发者工具
2. **审核发布**：提交审核，审核通过后发布

## 10. 技术支持

### 10.1 开发团队

- **后端开发**：负责后端API服务的开发和维护
- **前端开发**：负责前端管理界面的开发和维护
- **小程序开发**：负责微信小程序的开发和维护
- **测试团队**：负责系统测试和质量保证

### 10.2 联系方式

- **技术支持邮箱**：support@water-system.com
- **技术支持电话**：400-123-4567
- **GitHub仓库**：https://github.com/water-system/water-management

## 11. 版本历史

| 版本 | 日期 | 描述 |
|------|------|------|
| v1.0.0 | 2026-02-01 | 初始版本，完成基本功能开发 |
| v1.0.1 | 2026-02-22 | 完善文档，优化系统性能 |

## 12. 附录

### 12.1 环境变量配置

```env
# 数据库配置
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=123456
DB_NAME=water_system

# 服务器配置
PORT=8080
NODE_ENV=development

# JWT配置
JWT_SECRET=your_jwt_secret_key
JWT_EXPIRES_IN=7d

# 微信小程序配置
WECHAT_APPID=your_wechat_appid
WECHAT_SECRET=your_wechat_secret
```

### 12.2 开发规范

- **代码风格**：使用ESLint和Prettier进行代码风格检查
- **命名规范**：使用驼峰命名法
- **注释规范**：使用JSDoc注释
- **提交规范**：使用Conventional Commits

### 12.3 安全注意事项

- **密码加密**：使用bcrypt进行密码加密
- **JWT认证**：使用jsonwebtoken进行身份认证
- **SQL注入防护**：使用Sequelize ORM防止SQL注入
- **XSS防护**：使用Express的内置防护
- **CSRF防护**：使用JWT防止CSRF攻击
- **API权限**：使用中间件进行API权限控制

### 12.4 性能优化建议

- **数据库索引**：为频繁查询的字段添加索引
- **缓存机制**：使用Redis缓存热点数据
- **分页查询**：对大量数据使用分页查询
- **批量操作**：对批量数据使用批量操作
- **代码优化**：减少不必要的计算和IO操作
- **前端优化**：使用懒加载、代码分割等技术优化前端性能

---

**文档编写日期**：2026-02-22
**文档版本**：v1.0.1
**文档作者**：开发团队