# 饮用水领用系统开发文档 v1.0.5

## 1. 项目概述

饮用水领用系统是一个用于管理饮用水的领用、送水和库存的系统。系统包含前端管理界面、后端API服务以及小程序端，实现了完整的饮用水管理流程。

### 1.1 系统功能

- **仓库管理**：添加、编辑、删除仓库，仓库名称与存放位置自动同步
- **饮用水品类管理**：添加、编辑、删除饮用水品类
- **送水管理**：添加送水记录，审批送水记录，管理送水流程
- **领用管理**：提交领用申请，查看领用记录
- **库存管理**：查看库存状态，设置库存预警阈值
- **部门管理**：管理系统用户所属部门
- **用户管理**：添加、编辑、删除系统用户
- **系统日志**：记录系统操作日志

## 2. 系统架构

### 2.1 技术栈

- **前端**：Vue 3 + Element Plus + Vite
- **后端**：Node.js + Express + Sequelize
- **数据库**：MySQL
- **小程序**：微信小程序、支付宝小程序

### 2.2 目录结构

```
water-node+vue/
├── backend/           # 后端服务
│   ├── config/        # 配置文件
│   ├── middleware/    # 中间件
│   ├── models/        # 数据模型
│   ├── routes/        # 路由
│   ├── services/      # 业务逻辑
│   └── app.js         # 入口文件
├── frontend/          # 前端应用
│   ├── public/        # 静态资源
│   ├── src/           # 源码
│   │   ├── assets/    # 资源文件
│   │   ├── components/ # 组件
│   │   ├── router/    # 路由
│   │   ├── utils/     # 工具类
│   │   ├── views/     # 页面
│   │   ├── App.vue    # 根组件
│   │   └── main.js    # 入口文件
│   └── index.html     # HTML模板
├── water-mini-app/    # 微信小程序
└── water-alipay-mini-app/ # 支付宝小程序
```

## 3. 核心功能模块

### 3.1 仓库管理

- **功能**：管理仓库信息，包括仓库名称、位置、联系人等
- **特点**：
  - 仓库名称修改时，存放位置会自动同步更新
  - 仓库删除时，会检查库存是否为0，只有库存为0时才允许删除
  - 库存管理中显示的仓库名称会自动关联仓库管理中的设置

### 3.2 送水管理

- **功能**：管理送水记录，包括送水日期、数量、空桶数量等
- **特点**：
  - 送水记录需要审批（管理员可以直接审批）
  - 审批通过后会自动更新库存
  - 送水日期格式自动处理，确保数据正确性

### 3.3 领用管理

- **功能**：管理饮用水领用记录
- **特点**：
  - 领用记录关联仓库名称，直接显示仓库名称而非ID
  - 自动检查库存是否足够
  - 支持归还空桶

### 3.4 库存管理

- **功能**：管理饮用水库存，设置预警阈值
- **特点**：
  - 库存显示关联仓库名称，而非静态位置
  - 库存预警功能，当库存低于阈值时显示预警
  - 支持批量清零库存

## 4. 数据库结构

### 4.1 主要表结构

#### warehouse（仓库表）
| 字段名 | 数据类型 | 描述 |
|-------|---------|------|
| id | BIGINT UNSIGNED | 仓库ID |
| name | VARCHAR(100) | 仓库名称 |
| location | VARCHAR(255) | 存放位置 |
| contactPerson | VARCHAR(50) | 联系人 |
| contactPhone | VARCHAR(20) | 联系电话 |
| description | TEXT | 仓库描述 |
| createdAt | DATE | 创建时间 |
| updatedAt | DATE | 更新时间 |

#### inventory（库存表）
| 字段名 | 数据类型 | 描述 |
|-------|---------|------|
| id | BIGINT UNSIGNED | 库存ID |
| waterCategoryId | BIGINT UNSIGNED | 饮用水品类ID |
| warehouseId | BIGINT UNSIGNED | 仓库ID |
| quantity | INTEGER | 数量 |
| emptyBucketQuantity | INTEGER | 空桶数量 |
| location | VARCHAR(100) | 存放位置 |
| status | TINYINT | 状态 |
| alertThreshold | INTEGER | 预警阈值 |
| createdAt | DATE | 创建时间 |
| updatedAt | DATE | 更新时间 |

#### delivery_record（送水记录表）
| 字段名 | 数据类型 | 描述 |
|-------|---------|------|
| id | BIGINT UNSIGNED | 记录ID |
| waterCategoryId | BIGINT UNSIGNED | 饮用水品类ID |
| warehouseId | BIGINT UNSIGNED | 仓库ID |
| quantity | INTEGER | 数量 |
| emptyBucketQuantity | INTEGER | 领取空桶数量 |
| date | DATEONLY | 送水日期 |
| remark | VARCHAR(200) | 备注 |
| status | ENUM | 状态 |
| createdAt | DATE | 创建时间 |
| updatedAt | DATE | 更新时间 |

#### consumption（领用记录表）
| 字段名 | 数据类型 | 描述 |
|-------|---------|------|
| id | BIGINT UNSIGNED | 领用记录ID |
| waterCategoryId | BIGINT UNSIGNED | 饮用水品类ID |
| warehouseId | BIGINT UNSIGNED | 领用仓库ID |
| departmentId | BIGINT UNSIGNED | 领用部门ID |
| receiver | VARCHAR(50) | 领用人 |
| quantity | INTEGER | 领用数量 |
| returnEmptyBottles | INTEGER | 归还空桶数量 |
| consumptionDate | DATE | 领用时间 |
| notes | TEXT | 备注 |
| status | VARCHAR(20) | 状态 |
| createdAt | DATE | 创建时间 |
| updatedAt | DATE | 更新时间 |

## 5. API 接口

### 5.1 仓库管理接口

| 方法 | 路径 | 功能 |
|-----|------|------|
| GET | /system/warehouse | 获取所有仓库 |
| GET | /system/warehouse/:id | 获取单个仓库 |
| POST | /system/warehouse | 创建仓库 |
| PUT | /system/warehouse/:id | 更新仓库 |
| DELETE | /system/warehouse/:id | 删除仓库 |

### 5.2 送水管理接口

| 方法 | 路径 | 功能 |
|-----|------|------|
| POST | /delivery | 添加送水记录 |
| GET | /delivery | 获取送水记录列表 |
| GET | /delivery/:id | 获取送水记录详情 |
| PUT | /delivery/:id | 更新送水记录 |
| DELETE | /delivery/:id | 删除送水记录 |
| PUT | /delivery/:id/approve | 审批送水记录 |
| POST | /delivery/category | 添加饮用水品类 |
| GET | /delivery/category | 获取饮用水品类列表 |
| PUT | /delivery/category/:id | 更新饮用水品类 |
| DELETE | /delivery/category/:id | 删除饮用水品类 |

### 5.3 领用管理接口

| 方法 | 路径 | 功能 |
|-----|------|------|
| POST | /consumption | 提交领用申请 |
| GET | /consumption | 获取领用记录列表 |
| GET | /consumption/:id | 获取领用记录详情 |
| GET | /consumption/category | 获取饮用水品类列表 |
| GET | /consumption/warehouse | 获取仓库列表 |
| GET | /consumption/department | 获取部门列表 |

### 5.4 库存管理接口

| 方法 | 路径 | 功能 |
|-----|------|------|
| GET | /inventory | 获取库存列表 |
| GET | /inventory/alert | 获取库存预警 |
| PUT | /inventory/:id | 更新库存信息 |

## 6. 最近更新

### 6.1 版本 v1.0.5 更新内容

1. **仓库管理优化**：
   - 仓库名称修改时，存放位置自动同步更新
   - 仓库删除时，增加库存检查逻辑，只有库存为0时才允许删除
   - 库存管理中显示仓库名称，而非静态位置

2. **送水管理优化**：
   - 修复送水记录日期格式错误问题
   - 增加日期验证和处理逻辑

3. **领用管理优化**：
   - 领用记录直接显示仓库名称，而非通过ID查找
   - 优化部门名称显示

4. **库存管理优化**：
   - 库存页面显示仓库名称，与仓库管理关联
   - 支持批量清零库存

5. **系统稳定性**：
   - 修复外键约束错误
   - 优化数据库操作逻辑

### 6.2 系统运行环境

- **Node.js**：v14+
- **MySQL**：5.7+
- **前端**：Vue 3 + Element Plus
- **后端**：Express + Sequelize

## 7. 部署说明

### 7.1 后端部署

1. 安装依赖：
   ```bash
   cd backend
   npm install
   ```

2. 配置数据库连接（config/database.js）

3. 启动服务：
   ```bash
   node app.js
   ```

### 7.2 前端部署

1. 安装依赖：
   ```bash
   cd frontend
   npm install
   ```

2. 构建生产版本：
   ```bash
   npm run build
   ```

3. 部署dist目录到web服务器

## 8. 系统使用说明

1. **管理员登录**：使用系统管理员账号登录系统
2. **仓库管理**：在系统设置中添加和管理仓库
3. **饮用水品类管理**：在送水管理中添加和管理饮用水品类
4. **送水管理**：添加送水记录并审批
5. **领用管理**：提交领用申请
6. **库存管理**：查看库存状态和预警信息

## 9. 注意事项

1. 仓库删除前请确保库存为0
2. 送水记录需要审批后才会更新库存
3. 领用记录会自动检查库存是否足够
4. 系统会记录所有操作日志，便于后续审计

---

**文档版本**：v1.0.5
**更新日期**：2026-02-27
**开发团队**：饮用水领用系统开发组